import React from 'react';

const Test = ({name, age}) => {

    // const name = props.name
    // const age = props.age

  return (
  <div>      
      <p>Den här e-handelsbutiken heter {name} och har funnits i {age} år.</p>
  </div>);
};

export default Test;
